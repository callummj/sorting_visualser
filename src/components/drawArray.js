import React from "react";
import ReactDOM from "react-dom";
import './Visualiser.css';

/*
function drawArray(arrayy){
    const element = (
        <div id = "bars">
            {
                arrayy.map(i => (
                    <div key = {Math.random() * 100} className="arraybar"  style={{
                        height: `${i}px`,
                    }}/>
                ))}
        </div>
    );
    //ReactDOM.unmountComponentAtNode(document.getElementById('bars'))
    ReactDOM.render(document.getElementById('bars'), element);
}export default drawArray();*/